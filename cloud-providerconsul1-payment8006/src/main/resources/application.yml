server:
  # consul
  port: 8006
spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      # consul
      host: localhost
      port: 8500
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}

